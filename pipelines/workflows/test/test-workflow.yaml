apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: hello-
spec:
  entrypoint: hello
  serviceAccountName: argo-workflow

  templates:
    - name: hello
      container:
        image: alpine
        command: ["sh", "-c"]
        args: ["echo hello world"]
